### steps to install mavproxy in archlinux

```bash
sudo pacman -Syu --needed python python-pip opencv python-matplotlib python-wxpython python-lxml python-pygame
python -m pip install --user MAVProxy PyYAML
pip install opencv-python --user
pip install future
pip install wxPython
```
